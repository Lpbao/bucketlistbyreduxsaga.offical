

const styles = (theme) => ({
    taskBoard: {
        alignItems: "center",
        display: "flex"
    },
    button: {
        boderRadius: "4px",
        marginRight: "15px"
    }
})

export default styles